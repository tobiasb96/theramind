# TheraMind Environment Configuration

# OpenAI API Configuration (REQUIRED)
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Database Configuration (Optional - defaults to local PostgreSQL)
# Default uses Docker Compose PostgreSQL setup
# Format: postgres://username:password@host:port/database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/theramind

# Django Configuration (Optional - has defaults)
# Generate a new secret key for production: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-xy$#ixy%0b1onmp&)*r(*+o!*le+513jmuxc$0ezi8uu2zrp@=
DEBUG=True

# Application Settings
LANGUAGE_CODE=de-de
TIME_ZONE=Europe/Berlin

# Media and Static Files
MEDIA_ROOT=/path/to/media
STATIC_ROOT=/path/to/static